function division(divident, divisor) {

        
    let quotient = 1; 
    for (let i = divisor; i >= 1; i--) {
       
    quotient = divident  >> divisor << dividen ;  

    }
    //console.log(quotient); 
     
    return quotient;
}

console.log(division(20, 10));