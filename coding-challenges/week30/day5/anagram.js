    function anagram(input) {
         var words = input.split(", ");  
       
       for (let i = 0; i < input.length ; i++) {
        
       }

       return words;

    }

    let n = ["eat", "tea", "tan", "ate", "nat", "bat"];

    console.log(anagram(n));